/**
 * process.argv là một mảng (array) chứa các chuỗi (string).

Mảng này chứa các tham số được truyền khi chạy file Node.js.

Các phần tử trong mảng process.argv:

process.argv[0]: Đường dẫn tuyệt đối tới chương trình thực thi Node.js (ví dụ: /usr/local/bin/node hoặc C:\Program Files\nodejs\node.exe).

process.argv[1]: Đường dẫn tuyệt đối đến file JavaScript đang được chạy (ví dụ: /home/user/app.js).

process.argv[2] trở đi: Là các tham số dòng lệnh mà bạn truyền vào khi chạy chương trình.
 */
